# الختمة السمعية (Al-Khatmah Al-Sam'iyyah)

تطبيق Flutter بسيط وأنيق للاستماع إلى القرآن الكريم صفحة بصفحة مع إمكانية اختيار القارئ المفضل.

## ✨ المميزات

- 🎧 **الاستماع لكل صفحات المصحف** (604 صفحة)
- 🎙️ **10 قراء مشهورين** (العفاسي، السديس، عبد الباسط، المعيقلي، وغيرهم)
- ⚡ **تحكم في سرعة التشغيل** (1x - 3x)
- 🔄 **تشغيل تلقائي للصفحة التالية**
- 💾 **حفظ التقدم تلقائياً**
- 📊 **صفحة إحصائيات التقدم** مع دائرة تقدمية ورسائل تحفيزية
- 🌙 **واجهة عربية داكنة أنيقة**
- 🎨 **دعم RTL كامل**

## 📱 لقطات الشاشة

التطبيق يحتوي على 3 صفحات رئيسية:
1. **التقدم**: عرض إحصائيات الختمة مع دائرة تقدمية
2. **القرآن الكريم**: قائمة بجميع الصفحات مع مشغل صوتي سفلي
3. **الإعدادات**: إدارة القارئ، السرعة، والبيانات

## 🛠️ البناء والتشغيل

### المتطلبات

- Flutter SDK 3.0 أو أحدث
- Dart 3.0 أو أحدث
- Android Studio (للبناء على Android)
- Xcode (للبناء على iOS - Mac فقط)

### خطوات التشغيل

1. **استنساخ المشروع أو فك الضغط**
   ```bash
   cd a5tmy
   ```

2. **تثبيت الحزم**
   ```bash
   flutter pub get
   ```

3. **تشغيل التطبيق**
   ```bash
   # على محاكي أو جهاز متصل
   flutter run
   
   # أو تحديد المنصة
   flutter run -d android
   flutter run -d ios
   ```

4. **بناء APK للإصدار (Android)**
   ```bash
   flutter build apk --release
   # الملف سيكون في: build/app/outputs/flutter-apk/app-release.apk
   ```

5. **بناء App Bundle (للنشر على Google Play)**
   ```bash
   flutter build appbundle --release
   # الملف سيكون في: build/app/outputs/bundle/release/app-release.aab
   ```

6. **بناء IPA (iOS)**
   ```bash
   flutter build ios --release
   # ثم افتح المشروع في Xcode للتوقيع والتصدير
   ```

## 📦 الحزم المستخدمة

- `audioplayers: ^5.2.1` - تشغيل الصوت
- `shared_preferences: ^2.2.2` - حفظ البيانات محلياً
- `intl: ^0.18.1` - تنسيق التواريخ والأرقام

## 🎨 الألوان المستخدمة

- **خلفية داكنة**: `#0B1A2A`
- **بطاقات**: `#132539`
- **ذهبي**: `#E8C16B`
- **أخضر مائي**: `#78C8B1`

## 📂 هيكل المشروع

```
lib/
├── main.dart                    # نقطة الدخول
├── core/
│   ├── api_service.dart        # خدمة بناء روابط الصوت والقراء
│   ├── audio_service.dart      # خدمة إدارة الصوت المركزية
│   ├── theme.dart              # الثيمات والألوان
│   ├── constants.dart          # الثوابت
│   └── utils.dart              # دوال مساعدة
├── screens/
│   ├── startup/
│   │   └── startup_page.dart   # شاشة البداية
│   ├── listen/
│   │   └── listen_page.dart    # الشاشة الرئيسية للاستماع
│   ├── progress/
│   │   └── progress_page.dart  # صفحة التقدم
│   └── settings/
│       └── settings_page.dart  # صفحة الإعدادات
└── widgets/
    ├── bottom_player_bar.dart  # شريط المشغل السفلي
    ├── app_bar.dart            # شريط التطبيق المخصص
    └── setting_tile.dart       # عنصر الإعدادات
```

## 🔧 التخصيص

### تغيير الألوان
عدّل ملف `lib/core/theme.dart`:
```dart
class AppColors {
  static const Color gold = Color(0xFFE8C16B); // اللون الذهبي
  static const Color darkBg = Color(0xFF0E1A2A); // الخلفية
  // ...
}
```

### إضافة قارئ جديد
عدّل ملف `lib/core/api_service.dart`:
```dart
class Reciters {
  static const List<Reciter> all = [
    Reciter(
      id: "reciter_id",
      name: "اسم القارئ",
      baseUrl: "https://everyayah.com/data/reciter_id/PageMp3s/",
      imageUrl: "رابط_الصورة",
    ),
    // ...
  ];
}
```

## 🐛 حل المشاكل الشائعة

### مشكلة: الصوت لا يعمل
- تأكد من إضافة `INTERNET` permission في `AndroidManifest.xml` ✅ (تم إضافته)
- تحقق من اتصال الإنترنت

### مشكلة: الخط العربي لا يظهر بشكل صحيح
- تأكد من وجود ملفات الخط في `assets/fonts/` ✅ (موجودة)
- تحقق من `pubspec.yaml` أن الخطوط مسجلة بشكل صحيح ✅ (مسجلة)

### مشكلة: التطبيق يتوقف عند تغيير الصفحة
- تأكد من تثبيت جميع الحزم: `flutter pub get`
- نظف المشروع: `flutter clean && flutter pub get`

## 📝 ملاحظات التطوير

- التطبيق يستخدم `everyayah.com` API للحصول على ملفات الصوت
- التقدم يُحفظ تلقائياً في `SharedPreferences`
- التطبيق يدعم التشغيل في الخلفية عبر `audioplayers`
- تم إضافة ميزة التشغيل التلقائي للصفحة التالية
- تم إضافة حفظ واستعادة سرعة التشغيل

## 🚀 الخطوات التالية للنشر

### Google Play Store

1. **إنشاء Keystore للتوقيع**
   ```bash
   keytool -genkey -v -keystore ~/a5tmy-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias a5tmy
   ```

2. **إنشاء ملف `android/key.properties`**
   ```properties
   storePassword=كلمة_المرور
   keyPassword=كلمة_المرور
   keyAlias=a5tmy
   storeFile=/المسار/إلى/a5tmy-key.jks
   ```

3. **تعديل `android/app/build.gradle`** (إضافة التوقيع)

4. **بناء App Bundle**
   ```bash
   flutter build appbundle --release
   ```

5. **رفع على Google Play Console**

### Apple App Store

1. **فتح المشروع في Xcode**
   ```bash
   open ios/Runner.xcworkspace
   ```

2. **تعيين Bundle Identifier و Team**

3. **بناء وأرشفة**
   - Product > Archive
   - Distribute App

4. **رفع على App Store Connect**

## ✅ التحسينات المنفذة

### الصوت والتشغيل
- ✅ إضافة تشغيل تلقائي للصفحة التالية عند انتهاء الصفحة الحالية
- ✅ حفظ واستعادة سرعة التشغيل
- ✅ حفظ واستعادة القارئ والصفحة الحالية
- ✅ إضافة حدود للصفحات (1-604)
- ✅ معالجة الأخطاء بشكل أفضل

### واجهة المستخدم
- ✅ إضافة شريط الوقت (الحالي / الإجمالي) فوق السلايدر
- ✅ تحسين تصميم قائمة الصفحات مع حدود ذهبية للصفحة الحالية
- ✅ إضافة مؤشر تحميل دائري للصفحة الحالية
- ✅ إضافة مساحة سفلية كافية للبلاير في جميع الصفحات
- ✅ إضافة AppBar مع عنوان التطبيق
- ✅ تحسين الألوان والتباين

### صفحة التقدم
- ✅ دائرة تقدمية مخصصة تعرض النسبة المئوية
- ✅ بطاقات إحصائيات (الصفحة الحالية، الصفحات المتبقية)
- ✅ رسائل تحفيزية ديناميكية حسب التقدم

### صفحة الإعدادات
- ✅ عرض القارئ الحالي مع صورته
- ✅ عرض سرعة التشغيل الحالية
- ✅ مفتاح الوضع الداكن
- ✅ زر مسح التقدم مع تأكيد
- ✅ معلومات عن التطبيق

### التقنية
- ✅ دعم RTL كامل للعربية
- ✅ خط Cairo للعربية
- ✅ إضافة INTERNET permission
- ✅ تحديث اسم التطبيق في AndroidManifest

## 📄 الترخيص

هذا المشروع مفتوح المصدر للاستخدام الشخصي والتعليمي.

---

**تم التطوير بـ ❤️ باستخدام Flutter**
